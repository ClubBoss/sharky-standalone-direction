# Changes Log

1. Created the foundation recovery documentation structure and captured the active blockers/plan as the kickoff entry.
2. Added the Null telemetry backend/stub storage fallback, reworked `main.dart` with shared globals (navigatorKey, AppLanguageController, `appRoot`), fixed the glass player seat/tier-b parsing wrinkles, and aligned the widget smoke test with `AppRoot` so analysis/test commands can run.
