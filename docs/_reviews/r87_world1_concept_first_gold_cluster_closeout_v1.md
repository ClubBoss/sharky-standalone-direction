# R87 World1 Concept-First Gold Cluster v1 Closeout

## Purpose and bounded scope
- Expand R86 concept-first teaching from one micro-slice to the smallest adjacent reusable cluster on the same early authoritative path.
- Scope stayed bounded to:
  - `world1_act0_table_literacy` concept-first cluster step indexes `0-1` only.
  - authoritative runner seam + targeted contract update only.
- Out of scope respected:
  - no broad World1 rollout,
  - no World0 rewrite,
  - no theory framework/system,
  - no multi-street/sizing,
  - no map/result changes,
  - no schema/validator expansion.

## PIEC summary
- Reconciled:
  - `docs/_reviews/r86_world1_concept_first_gold_micro_slice_closeout_v1.md`
  - `docs/_reviews/r85_world1_gold_learning_literacy_cluster_closeout_v1.md`
  - `docs/_reviews/r84_world1_gold_learning_literacy_slice_closeout_v1.md`
  - `docs/_reviews/r83_gold_learning_authoring_contract_v1.md`
  - `docs/_reviews/r77_scenario_truth_foundation_v1.md`
- Selected adjacent cluster:
  - concept-first seat cluster step indexes `0-1` on `world1_act0_table_literacy`.
- Why this cluster:
  - narrowest adjacent reuse beyond R86,
  - same deterministic seat-identification path,
  - high pedagogical value without spreading concept framing by inertia.

## Fields and runtime reuse
- No new truth fields were added.
- Reused existing fields/runtime composition:
  - concept setup from `instructionText`,
  - `Why it matters:` framing from `goalText`,
  - explicit focus cue from expected seat target ids,
  - factual incorrect correction via seat mismatch path + `Why:` line,
  - compact correct reinforcement via deterministic `Reinforce:` line.

## What changed
- Runtime cluster gate:
  - concept-first seat framing expanded from step `0` only to steps `0-1` only.
- Targeted contract:
  - verifies covered step keeps concept-first pre-click framing after advancing,
  - verifies no concept-first leakage onto uncovered adjacent step `2`.

## User-visible effect
- On covered concept-first cluster steps (`0-1`), learner now consistently sees:
  1. short concept statement,
  2. short `Why it matters`,
  3. explicit `Notice` cue,
  4. identify/select task,
  5. factual `Why` on incorrect,
  6. compact `Reinforce` on correct.
- Uncovered adjacent step (`2`) remains unchanged to avoid copy spread.

## Defer note
- This milestone proves bounded adjacent concept-first reuse only.
- Any broader concept-first rollout remains deferred to future bounded milestones.
