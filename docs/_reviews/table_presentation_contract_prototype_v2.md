# Table Presentation Contract Prototype V2

## 1. Terminal verdict

**F — seam extraction was safe; V2 stopped before comparison.** Phase A is a
behavior-preserving source-ownership extraction. Phase C's first exact-atom
feedback projection overflowed its constrained evidence region, so it failed the
production-fidelity fit gate. No V2 candidate score, visual winner, production
pilot, or public claim is admitted.

## 2. Starting branch/head/local stack

Started at `b1e9a11c` on `main`, eight commits ahead of `origin/main`. Existing
local ancestry remains unchanged; Phase A is `7b5d581b`.

## 3. Phase A source extraction

Added `Act0TablePresentationConfigV1` and `Act0TableSceneV1`. The runner now
uses a private `_Act0TableSceneRunnerInput` adapter that forwards every existing
private policy input to the same `_Act0TableV1` leaf. No runner-private enum is
publicly exposed.

## 4. Exact files and ownership boundaries

- `lib/ui_v2/act0_shell/act0_table_presentation_config_v1.dart`: data-only
  presentation config; no route, progress, correctness, telemetry, or private
  runner types.
- `lib/ui_v2/act0_shell/act0_lesson_runner_shell_v1.dart`: public scene wrapper
  and private runner adapter; existing private renderer and visual atoms remain
  source of current production output.

## 5. Production equivalence proof

The adapter is one-to-one forwarding into `_Act0TableV1`; no visual math,
tokens, seat/card/bet/marker composition, overlay, or route path changed.
Focused compact-layout, W7-W12 table-context/W9 lane, and progress-scope tests
passed after extraction. This is structural/runtime equivalence proof; no new
release-evidence screenshots were generated or promoted.

## 6. Shared semantic-state proof

The public scene accepts the existing `Act0TableStateV1`. It derives no poker
truth and leaves runner selection/replay/repair/route policy private.

## 7. Candidate definitions

Candidate 0 would be current runner composition. Candidate 1 would wrap exact
`Act0TableSceneV1` in a state-aware outer budget. Candidate 2 would use that
scene for decisions plus an exact-atom evidence projection and table-free
information mode. Only the extraction prerequisite was completed.

## 8. Final layout budgets

Not selected. Proposed ranges remain audit hypotheses: decision table 46–56%,
feedback full-table 36–45%, feedback evidence 30–42%, and deterministic lower
zones/CTA anchors. No raw text measurement is permitted.

## 9. State/content-class matrix

The planned W1 decision/correct/wrong, repair result/open source, W9, W12,
No-W13, Welcome demo/handoff matrix was not rendered after the fit failure.

## 10. Screenshot inventory

None for V2. Existing V1 local evidence remains unmodified as historical local
prototype evidence only.

## 11. Visible-occupancy methodology

V2 must measure keyed allocated/visible table and lower bounds, CTA, declared
breathing room, largest background-connected unexplained blank band, occupancy,
and visual-mass center. It must not equate allocated zone with visible content.

## 12. Full metric table

Not produced: the exact-atom feedback projection had a RenderFlex overflow
before a valid candidate frame existed.

## 13. Per-state comparison

Not produced. Continuing would have made schematic/overflowing evidence look
like a production-quality result.

## 14. Per-candidate scoring

Not applicable; no candidate passed the visual-fidelity gate.

## 15. Objective defect totals

The stopped projection had one constrained-region bottom overflow. No stable
V2 clipping, overlap, occupancy, identity, or blank-band totals exist.

## 16. Identity comparison

Phase A config supports target identity policy data, but production identity was
not changed and no V2 comparison passed. `You · BTN` remains prototype target,
not production output.

## 17. Production visual-fidelity assessment

Decision scene extraction preserves exact production scene internals. Feedback
evidence is **not yet production-quality**: reusing all exact center/card atoms
inside a short evidence region requires a separately bounded visual composition
contract, not direct stacking of full-scene-sized children.

## 18. Winner/no-winner decision

No winner. The stop condition “evidence projection looks schematic or cannot
reach production-quality cohesion” applies.

## 19. Why each rejected candidate lost

Current and state-budget candidates were not re-scored because V2's shared
three-candidate evidence set is incomplete. Three-mode cannot win while its
feedback evidence submode overflows.

## 20. Recommended first production pilot

None admitted.

## 21. Exact pilot state family

None.

## 22. Exact pilot non-scope

No decision, feedback, repair, Welcome, or terminal migration; no route,
telemetry, W13, content, Sharky, motion, tablet, dependency, or Modern Table
work.

## 23. Compatibility with existing local commits

`1387f3c9` feedback clue, `10c3c720` W9 overlay, and `a341868b` progress scope
remain valid and were revalidated by focused guards. `15ecd437` remains local
composition evidence. No prior commit was altered.

## 24. Risks

Forcing exact full-scene atoms into evidence mode repeats the V1 allocation
mistake at a smaller scale. Exporting private renderer helpers or adding a debug
route would broaden API/route scope. A future projection needs explicit,
production-token-sized evidence atoms rather than unbounded full-scene stacks.

## 25. Extraction rollback

`git revert 7b5d581b` reverses the one behavior-preserving extraction commit.
It does not alter prior local history.

## 26. Prototype deletion instructions

No V2 harness/output survived the stop. Remove any future local V2 directory
with `rm -rf output/prototypes/table_presentation_contract_v2`.

## 27. Evidence paths

No V2 evidence path. V1 local evidence remains at
`output/prototypes/table_presentation_contract_v1/` and is not release evidence.

## 28. Human QA/public implications

No Human QA, 10/10, public-readiness, tablet-quality, or learning-effect claim
is made.

## 29. Non-claims

This does not claim Phase A selects a presentation mode or approves a pilot.

## 30. Next prompt

No production-pilot prompt is included. The next task must first design and
unit-test bounded production-token evidence atoms (cards, compact pot/price,
identity, cue) with exact size contracts before attempting V2 again.
