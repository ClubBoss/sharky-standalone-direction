# R85 World1 Gold Learning Literacy Cluster v1 Closeout

## Purpose and bounded scope
- Expand R84 literacy framing from one slice to the smallest adjacent reusable cluster on the same path.
- Scope stayed bounded to:
  - `world1_spine_campaign_v1` early actionable steps `0-1` only.
  - authoritative runner seam + targeted contract updates only.
- Out of scope respected:
  - no broad World1 rollout,
  - no theory framework,
  - no multi-street/sizing work,
  - no schema/validator expansion.

## PIEC summary
- Reconciled:
  - `docs/_reviews/r84_world1_gold_learning_literacy_slice_closeout_v1.md`
  - `docs/_reviews/r83_gold_learning_authoring_contract_v1.md`
  - `docs/_reviews/r82_world1_gold_learning_cluster_closeout_v1.md`
  - `docs/_reviews/r77_scenario_truth_foundation_v1.md`
- Selected cluster:
  - two-step adjacent literacy cluster: step indexes `0` and `1`.
- Why this cluster:
  - minimal adjacent proof beyond single slice,
  - keeps literacy framing high-EV and non-spammy,
  - avoids forcing literacy copy onto step 2 where added value is weaker.

## Fields and runtime reuse
- No new fields required.
- Reused existing fields and truth composition:
  - concept setup: `contextText`
  - value framing: `tradeoffText` (`Why it matters: ...`)
  - focus cue: `requiredFocusLabelV1`
  - factual incorrect correction: `whyV1`
  - compact reinforcement: `insightText` (selective existing policy)

## What changed
- Runtime:
  - literacy line gate expanded from step `0` only to steps `0-1` only.
- Contracts:
  - targeted gold contract updated to require literacy line on covered steps and deny leakage onto uncovered adjacent step (`2`).

## User-visible effect
- On first two actionable `world1_spine_campaign_v1` steps, learner now sees:
  - short setup,
  - short `Why it matters:` value framing,
  - explicit focus cue,
  - action task,
  - factual incorrect why,
  - selective compact reinforcement behavior unchanged.
- Step 3 (index 2) remains non-literacy-framed to keep copy tight.

## Defer note
- This is a bounded adjacent literacy cluster proof only.
- Broader literacy rollout remains deferred to future bounded milestones.
