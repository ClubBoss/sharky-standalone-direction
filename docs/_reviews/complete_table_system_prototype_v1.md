---
status: "undeclared"
status_source: "absent"
generated_by: "docs_frontmatter_v1"
---

# Complete Theory-to-Task Table System Prototype v1

## 1. Terminal verdict

The isolated production-fidelity harness passed its deterministic capture,
geometry-lock, overflow, and corrected theory-occupancy checks. **No theory
winner and no production pilot are selected.** Two stop conditions remain:
the separately owner-supplied current theory screenshot could not be
identified/inspected, and the exact current scene still renders `BTN Hero` plus
a dealer disc rather than the requested learner-facing `You · BTN` contract.

## 2. Prototype isolation

Only `test/prototypes/complete_table_system_v1_test.dart` and this review are
tracked. The harness is not imported by app code, uses no route, telemetry,
dependency, or production-source change, and writes all PNGs/metrics under the
untracked local evidence root.

## 3. Attached visual authorities reviewed

`Theory & Complete System Review.pdf` and `Table Continuity Review.pdf` were
visually rendered and inspected. Both support a deliberate theory→practice
boundary, an exact shared table scene, and a locked task loop. The former's
80%-theory visual recommendation conflicts with this prompt's 88/90/92
comparison range; the active prompt range was used. The owner current-theory
screenshot referenced by the prompt/addendum was not identifiable as a local
attachment, so it was not substituted with unrelated files.

## 4. Shared hand/state proof

Every spatial capture uses one `Act0TableStateV1` and exact
`Act0TableSceneV1`: A♠ K♦, A♥ 7♣ 2♦, `Pot 3 BB`, `Call 1 BB`, the same six
seats, BTN/dealer facts, and one `tableContextKey` concept. No candidate gains
space by changing poker truth or replacing the table renderer.

## 5. T1/T2 comparison

All theory candidates now use the same corrected bottom-anchored,
content-sized panel. They therefore compare table geometry—not a fixed-slot
T1 against a content-sized T2. T1 is 344px wide; T2 uses 88%, 90%, and 92% of
that same full geometry with one fixed raised anchor per candidate.

## 6. Exact scale comparison

| Candidate | Table width | Table height | Panel height | Footer gap | Occupancy |
| --- | ---: | ---: | ---: | ---: | ---: |
| T1 | 344.00 | 597.22 | 162 | 18 | 83.3% |
| T2-88 | 302.72 | 525.56 | 162 | 18 | 83.3% |
| T2-90 | 309.60 | 537.50 | 162 | 18 | 83.3% |
| T2-92 | 316.48 | 549.44 | 162 | 18 | 83.3% |

All candidates keep the same 3–4 line theory copy, no scroll, and a reachable
CTA. The addendum's removal of unused panel reserve lets T1 fit the content
contract too; T2 no longer wins merely by avoiding an artificial lower void.

## 7. Theory winner or no-winner

No winner. T1 is viable after the occupancy correction and visually has the
largest exact production table. T2-92 is the closest raised alternative and
is visibly distinct but not miniature. The missing owner control and the
identity blocker prevent a valid final selection.

## 8. Theory content fit

The compact/standard theory cards use one eyebrow, a two-line title ceiling,
two/four-line body ceiling, an 18px body-to-footer gap, one control row, and
no scroll. Excess copy is represented as an authored split/rewrite guard, not
as a table resize or another scroll region.

## 9. Theory blank-space result

The deliberately bad fixed-slot control records a 248px panel, a 108px
body-to-footer gap, and 66.1% occupancy. Corrected candidates record 162px,
18px, and 83.3%. The correction recovers 86px of panel allocation for this
fixture without reducing table geometry by content length.

## 10. Theory readability

Cards, board, pot, labels, seats, and felt are exact scene output in all
candidates. T1 is clearest by size; T2-88 is visibly smallest; T2-90/92 remain
legible in the rendered comparison. The prototype cannot certify the required
learner identity because the exact current scene visibly uses `BTN Hero` and a
dealer disc.

## 11. Theory beat geometry lock

Hero, board, pot, and recap beats at T2-90 all measured table top 42px, width
309.6px, and height 537.5px. Repeated PNG bytes were identical per fixture.
No beat selected geometry from copy length, locale, or control content.

## 12. Learn→Apply boundary

The harness captures a static final-theory state, a full task-loop destination,
and first decision with the same hand. It is a source-declared prototype
boundary only; no animation was implemented. The visual order is theory panel
exit → full table destination → decision sheet entry. Accessibility focus is
specified for future implementation as the destination decision heading.

## 13. Task-loop geometry lock

Decision through recheck all measure table top 42px, width 344px, and height
597.22px. The same exact scene is rendered for correct/wrong feedback, repair,
detail, Peek, restore, result, and recheck; state changes alter only the panel.

## 14. Sheet states

Peek is 70px and has a persistent Show explanation control. Compact is 206px.
Standard is 304px. Expanded is 430px. The prototype uses explicit controls and
no drag, swipe, timer, or zero-height state.

## 15. CTA-in-Peek matrix

Correct feedback may expose Continue in compact. Peek exposes Show explanation.
Wrong/repair/retry progression remains conceptually blocked until explanation
is restored. Theory has no Peek state; decision/recheck retain answer flow.

## 16. Critical-element visibility

The declared always-visible set is board, pot, call price, and active clue;
compact/standard additionally require hero cards, relevant seat, position, and
task-defining bet. The prototype's exact table was not cropped and no captured
fixture had table-bound overflow. A future production sheet must calculate
actual critical bounds rather than rely on this static prototype assertion.

## 17. Conditional evidence header

No feedbackEvidence renderer was built. The evidence-header contract is
recorded in `occlusion_metrics.json`: use it only when an Expanded sheet would
cross actual critical bounds, restating only board, hero cards, You/position,
and pot/price. This harness does not claim a production header implementation.

## 18. Content-guard results

The generated guard report records theory compact ≤2 body lines, standard ≤4,
no theory scroll, 2–4 decision options at ≤2 lines, and an explicit
rewrite/split outcome for long copy. No geometry responds to the guard class.

## 19. Complete state coverage

Captured: four theory candidates; four locked theory beats; corrected panel and
fixed-slot control; static boundary trio; nine task-loop states; four-option,
W9, position, hero-card, dealer-order, and localized stress representations;
information theory; terminal information; and safe-overlay visualization.

## 20. Stress controls

Stress captures are deterministic and overflow-free after the Peek repair. They
exercise the shared scene/panel boundary, not new W9, dealer, identity, or
localization production semantics. The long-copy case documents a content
guard rather than accepting scrolling options.

## 21. Metrics

`geometry_metrics.json` records panel/table/text/footer/CTA bounds, body gap,
occupancy, reachability, overflow, and repeated-render result for 33 captures.
Every corrected theory panel passes the 16–24px gap and ≥80% occupancy gates.
Every captured task-loop state has identical table bounds within zero logical
pixels. No test capture reports overflow or clipping.

## 22. Visual credibility

The exact public production scene provides authentic felt, cards, seats, pot,
and labels, avoiding the V1 simplified projection limitation. The prototype is
still a local test harness, not a canonical runner rendering: interaction,
source metadata, target identity, and real sheet bounds remain to be proven in
a separately admitted production pilot.

## 23. Remaining UX uncertainties

- Does the missing owner current-theory screenshot reveal a composition issue
  not represented by this shared fixture?
- Is a 92% raised theory table more intentional than T1 to actual learners,
  rather than simply smaller?
- How should the production scene implement `You · BTN` while preserving a
  dealer disc only for dealer-order lessons?
- Which actual production critical-bounds cases require the conditional header?

## 24. Whether motion prototype is needed

No. Static before/after states establish the state-machine and geometry
contracts. Motion is a later bounded concern only after the theory winner,
identity, and production-seam gates are resolved.

## 25. Whether one bounded production pilot is admitted

**No.** The owner screenshot stop condition and exact-scene identity contract
are unresolved. The prototype may not promote itself to a production pilot.

## 26. Exact pilot scope if admitted

Not admitted. A future prompt must first attach/identify the current theory
control, resolve the learner-facing identity seam without duplicating the
scene, select T1 or a T2 scale from valid evidence, and then admit one
same-hand theory→decision family only.

## 27. Exact non-scope

No production migration, canonical route change, runner rewrite, motion,
telemetry, dependencies, feedbackEvidence renderer, persistent preference,
tablet/landscape behavior, Human QA, or public readiness claim.

## 28. Rollback/deletion

Delete `test/prototypes/complete_table_system_v1_test.dart` and
`output/prototypes/complete_table_system_v1/` to remove the prototype. No
production file requires restoration.

## 29. Human QA/public non-claims

No Human QA was run. This work does not claim learner preference, learning
effectiveness, accessibility certification, premium/10-of-10 quality,
retention, conversion, launch readiness, or a selected production direction.

## 30. One exact next prompt only if all relevant gates pass

Not included: all relevant gates did not pass. The next action is to provide
the missing owner current-theory screenshot and separately admit/resolve the
`You · BTN` production-scene identity seam before requesting another prototype
or pilot.
