# Canonical Journey Truth & Progression Integrity v1

Status: ACTIVE route-truth correction.

## Decision

The Alpha Learning Loop capability remains valid. Its prior contract was not:
real iOS Simulator Computer Use proved that a fresh learner does not travel
directly from Learn to `Fold, check, call, raise`.

The source-owned W1 order is intentional:

1. First Table Guide (`what_poker_is`)
2. What poker is (`what_poker_is_content`)
3. Cards, ranks & suits (`cards_ranks_suits`)
4. Your first hand, dealt (`your_first_hand`)
5. Fold, check, call, raise (`fold_check_call_raise`)

At fresh Learn state, the first lesson is current and Action is locked. Action
becomes current only after every task in the preceding four lessons is recorded
as complete in the normal persisted progression model.

## Contract correction

- Retired: `alpha_action_repair_recovery_v1`; it falsely described direct
  fresh-install Action reachability.
- Fresh learner truth: `alpha_fresh_install_first_lesson_v1`.
- Action capability truth: `alpha_action_repair_recovery_unlocked_v2`, with
  `alpha_action_capability_prerequisite_progression_v1` as its validated,
  production-equivalent starting history.

The Action fixture contains only completed prerequisite learner history; it
starts before Action entry and cannot enter Action feedback, repair, or recheck
directly. It is valid deterministic capability setup, not fresh-user evidence.

## QA admission rule

The factory emits separate fresh-install and Action-capability verdicts. It
rejects the retired contract, unmet Action preconditions, Dev/debug shortcuts,
and direct-state black-box evidence. Raster capture remains separately labeled
source-owned state evidence and is not presented as learner-route proof.
