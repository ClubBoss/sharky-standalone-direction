# PHP-1 Stale Contract Non-Weakening v1

## 1. Exact baseline

- Canonical baseline: `e765ec0d58bc575d66dc60f8bd96d9b7a46aa0c2` (`origin/main`, PR #55).
- Candidate branch: `codex/php1-stale-contract-nonweakening-v1`.
- Candidate worktree: `/private/tmp/sharky-php1-stale-contract-nonweakening-v1`.
- The user checkout was already dirty with unrelated documentation work and was not modified.

## 2. PHP-0 inherited facts

PHP-0 closed F-17 as `CLASSIFIED`: required 0, stale 2, active-nonblocking 5,
archived 7, quarantined 29, unresolved-unique 10. F-18 is `STALE_TEST`: the
compact fixture's teaching-step expectation failed identically from `4563ce2d`
through the PHP-0 head and is not a PR #51/#52 production regression.

## 3. Phase-7 stale assertion

`phase_7_closure_audit_contract_test.dart` still asserted a superseded
Phase-8/Motion capsule snapshot, including a historical active-route artifact.
The stale wording was introduced by `4d424a8b` and was superseded by the
campaign-publication overlay merged in PR #55 (`e765ec0d`).

## 4. Phase-7 replacement authority

The replacement authority is the Node-5 campaign plan, the reconciled campaign
state, and the current-execution section of `ACTIVE_ROUTE_CAPSULE_v1.md`. The
guard now requires the Node-5 contract-restoration stage, PHP-1 state, F-17
classification, F-18 stale-test result, Human Proof prohibition, closed Phase
7, no reopening of closed capabilities, and Modern Table Maintenance Mode.

## 5. Phase-7 non-weakening verdict

**NON_WEAKENING.** The repair removes only stale historical-state literals and
retains exact semantic prohibitions. It strengthens execution authority by
requiring the current campaign state rather than accepting generic capsule text.

## 6. Compact-decision stale fixture

The target fixture enters `Act0LessonPhaseV1.theory` for
`what_poker_is_table_read_recheck`, but does not select the `teachingSteps`
entry containing `Same scan, different spot.`. The original assertion therefore
tested an unentered page. Its failure predates F-16 and remains a fixture/retired
expectation mismatch, not a rendering defect.

## 7. Compact replacement authority

`test/ui_v2/act0_instruction_content_policy_v1_test.dart` owns the current
instruction compactness policy. The surviving compact-decision test owns the
independent learner-visible composition contract: `Read the table first`, no
learning-rail scroll view, table/lower-stage adjacency, fixed footer, reachable
Continue CTA, and no framework exception.

## 8. Compact non-weakening verdict

**NON_WEAKENING.** Treatment C was used: only the stale teaching-step assertion
was removed, with its replacement authority named in-source and here. The test's
no-scroll/reachability/table-geometry contract remains intact.

## 9. F-16 item-by-item matrix

| Items | Original failing assertion / old contract | Retirement or supersession | Current production behavior and replacement authority | Strength preserved | Verdict |
| --- | --- | --- | --- | --- | --- |
| #4 | No-proof fallback expected retired receipt copy. | `b93d9d34` replaced the assertion after the current fallback was admitted. | The band-transition surface requires `Repair result saves the next time you fix one.` in `act0_w4_w5_band_transition_milestone_v1_test.dart`. | Exact visible fallback remains asserted. | **NON_WEAKENING** |
| #5 | Fixture expected reinforced proof without qualifying transfer evidence. | `b93d9d34` supplied current qualifying evidence. | The current fixture creates transfer evidence and asserts the exact reinforced proof icon. | Banked-proof coverage was retained, not deleted. | **NON_WEAKENING** |
| #6 | Retired `improved_v1` fixture stood in for canonical transfer verdict. | `b93d9d34` updated it to the current verdict/valid observation. | `act0_repair_outcome_consumer_v1_test.dart` asserts scenario-specific acknowledgement from a valid proof. | Learner-visible receipt enrichment remains exact. | **NON_WEAKENING** |
| #7–9 | W9/W10 assertions used retired route identity/order. | `b93d9d34` aligned the assertions; `40968e4a` added bounded transfer acknowledgement evidence. | `act0_w9_w10_internal_world_source_template_batch_v1_test.dart` asserts the admitted Player Adjustment four-task identity/order and source-ID resolution. | Route identity is semantic and source-backed, not a loose label check. | **NON_WEAKENING** |
| #11 | Session Summary expected a retired value key. | `b93d9d34` replaced it. | `session_result_world1_onboarding_payoff_test.dart` requires exactly one `session_result_up_next_headline_v1` next-capability surface. | Learner-visible next value remains required. | **NON_WEAKENING** |
| #12 | The literal `Start Volume I` CTA was expected after its deliberate retirement. | `5c356710` retired the overclaim; `460f2eed` corrected the guard. | `act0_visual_ux_known_p1_copy_contract_test.dart` requires localized `Volume I`/`Foundations` and forbids `Start Volume I` plus the vague 36-world claim. | Forbidden-copy protection is preserved and strengthened. | **NON_WEAKENING** |

The independent source/history review also verified that the separate Sharky
guard still proves at-most-once acknowledgement, and that compact instruction
policy remains scoped to instruction rendering rather than receipt/support copy.

## 10. Any weakened assertions found

None.

## 11. Repairs made

Two stale assertions were corrected: the Phase-7 guard now targets current
campaign authority, and the compact fixture no longer claims an unselected
teaching step renders. No F-16 repair was needed.

## 12. Production-change statement

No production source, authored content, progression, telemetry, motion, Sharky,
accessibility, or Modern Table behavior changed.

## 13. Focused validation

The two directly changed files and all eight named F-16 protection files pass
on the candidate: **82 tests passed**.

## 14. Global validation

`flutter analyze` passed with no issues. `graphify hook-check` and `git diff
--check` passed. The Tier-A manifest validator and Tier-A lane passed. The
quick R5 readiness preflight passed; the R5 release gate ran its analyzer and
selected fast-loop checks on this candidate.

## 15. Exact-head CI

Pending PR creation and exact-head required CI: Tier A, Health, Theory
Integrity (expected no-theory-change skip), and R5 release gate.

## 16. Campaign state update

The state now records the PR #55 baseline, PHP-0 `CLOSED_CLASSIFIED`, PHP-1
authorization, the two stale assertions, F-16 review result, and the rule that
the final merge SHA is recorded here and in GitHub only after merge.

## 17. Deferred packet routing

Stop at `PENDING_OWNER_GATEWAY`. PHP-2 and PHP-3 remain unauthorized; no
legacy disposition, manifest, CI workflow, visual, motion, Sharky, native, or
Human Proof work entered this packet.

## 18. Final PHP-1 verdict

**PHP1_CLOSED_NON_WEAKENING** — contingent on the exact-head CI and merge gates
in sections 13–15.

## Evidence ledger

| Claim | Required proof | Observed evidence | Class | Exact provenance | Verdict / remaining gate |
| --- | --- | --- | --- | --- | --- |
| Phase-7 repair is current and non-weakening | current authority plus focused guard | historic commit, current campaign/state/capsule, passing guard | deterministic | candidate worktree at PHP-1 head | CI pending |
| Compact repair preserves compact behavior | focused test and policy replacement | pre-change stale failure; surviving geometry/no-scroll assertions; policy guard | deterministic | candidate worktree at PHP-1 head | CI pending |
| Seven F-16 stale adjudications are non-weakening | source/history and all protection tests | matrix above and required test set | deterministic | candidate worktree at PHP-1 head | CI pending |
