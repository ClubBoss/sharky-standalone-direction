# B7 Character Slice Reconciliation + Scene Geometry Admission v1

Status: AUTHORITY / ACTIVE DISPATCH
Freshness date: 2026-08-19
Repository: `ClubBoss/sharky-standalone-direction`

## Purpose

Reconcile the completed B7 character vertical-slice evidence, record the newly
proven table-geometry stability defect, and admit exactly one next bounded B7
structural wave:

`B7_CANONICAL_SCENE_COMPOSITION_AND_GEOMETRY_V1`.

This authority does not implement product code, does not canonize prototype
character assets, does not admit a full character pack, does not run P02, does
not reopen HNP, and does not admit B8.

## Starting canonical state

Starting canonical main:

`3c815afcd1eb285fb21535f1ba8347e677add7ea`

Resolved dispatch before this reconciliation:

- `B7 = ACTIVE / ADMITTED`
- `B7_ITERATION_1_CODE_COHESION_V1 = CLOSED_PASS`
- `B7_CHARACTER_VERTICAL_SLICE_V1 = ACTIVE / ADMITTED`
- `EXACT_NEXT_ACTION = IMPLEMENT_B7_CHARACTER_VERTICAL_SLICE_V1`
- `TEXT_SCALE_POLICY_V1 = SINGLE_CANONICAL_PRODUCT_SCALE`
- `P02 = DEFERRED / NOT_STARTED`
- `HNP_HARNESS = CLOSED_UNCHANGED`
- `HUMAN_PROOF = FALSE`
- `B8 = NOT_ADMITTED`

## Character vertical-slice reconciliation

Returned evidence establishes:

`CHARACTER_ASSET_ARCHITECTURE = PROVEN`

Authored transparent 2D / 2.5D PNG assets can ride the existing deterministic
scene architecture without another scene system. The slice proved compatibility
with:

- B1 anchors and projection;
- rail occlusion;
- B2 lighting relationship;
- B5 recession;
- B6 semantic motion;
- deterministic capture.

The visual comparison also establishes:

`CLASS_LEVEL_LIFT = YES`

The authored opponent and first-person hero foreground materially outperform the
procedural B3 baseline in human presence.

The returned art itself is not production-approved:

- `CURRENT_SCRIPT_GENERATED_ART_QUALITY = PROTOTYPE_ONLY`
- generated opponent = `REWORK`
- generated hero = `REWORK`
- Python/PIL production method = `NOT_APPROVED_FOR_FULL_PRODUCTION_PACK`
- `PREMIUM_CHARACTER_ART = NOT_YET_PROVEN`
- `ART_PRODUCTION_PIPELINE = REQUIRES_SEPARATE_SELECTION`
- `FULL_CHARACTER_PACK = NOT_STARTED`
- `FULL_CHARACTER_PACK = NOT_ADMITTED`

Canonical disposition:

`B7_CHARACTER_VERTICAL_SLICE_V1 = CLOSED_PASS_PROOF`

The slice closed the architecture / asset-hypothesis question only. Its current
prototype PNGs must not be merged or treated as final character art.

## Table geometry forensic reconciliation

Codex forensic evidence proves:

`TABLE_GEOMETRY_STABILITY = NOT_PASSED`

Root cause:

`B7_ITERATION_1_FEEDBACK_SURPLUS_POLICY = DIRECT_CAUSE_OF_STATE_DEPENDENT_TABLE_SCALE`

B7 Iteration 1 allows review / feedback states to claim additional table-height
surplus. The same table renderer therefore receives a larger `maxTableHeight`
during feedback/review, causing the whole physical poker world to rescale.

The affected physical geometry includes:

- outer table;
- felt;
- seats;
- character / hero anchors;
- board;
- pot;
- physical table-space anchors.

The character vertical slice is not the cause.

This does not reopen B7 Iteration 1 as failed. Its accepted center, chrome,
compact-commitment, text-scale and feedback-surface gains remain valid.

Classification:

`TABLE_GEOMETRY_STABILITY = MATERIAL_GAP_CONFIRMED`

## Canonical table invariant

Record:

`TABLE_GEOMETRY_INVARIANT_V1 = REQUIRED`

For one viewport at the canonical text scale:

`ONE VIEWPORT -> ONE PHYSICAL TABLE GEOMETRY -> SAME GEOMETRY ACROSS THE LEARNING LOOP`

The invariant applies across:

- decision;
- correct feedback;
- wrong feedback;
- repair;
- recheck;
- continuation / result.

State may change:

- salience;
- opacity / recession;
- clue state where existing semantics authorize it;
- HUD semantic state;
- feedback copy;
- dock content and visible height.

State may not change:

- scene camera;
- physical table envelope;
- table scale;
- projected table perspective;
- seat anchors;
- character anchors;
- board / pot / hero-card physical anchors;
- rail geometry.

## Canonical scene direction

Independent composition review of current decision, feedback, character-slice
evidence, the premium Sharky concept and PokerSkill as a structural benchmark
selects:

`RECOMMENDED_CANONICAL_SCENE_DIRECTION = B_STRONG_PREMIUM_LIFT`

Do not freeze either current decision geometry or current feedback geometry.
The target is a new canonical geometry:

- wider physical table than current decision;
- vertically shorter projected table than current decision;
- lower far rail;
- near rail roughly preserved;
- materially more far-player / teaching breathing room;
- stronger perspective / foreshortening;
- board and hero cards kept near current decision readability.

Indicative design envelope, to be tuned from exact renders rather than copied as
blind constants:

- felt width: approximately `66-72%` viewport;
- outer table width: approximately `78-84%` viewport;
- outer table projected height: approximately `46-50%` viewport;
- table center: approximately `54-56%` viewport Y;
- far rail: approximately `31-34%` viewport Y;
- near rail: approximately `75-78%` viewport Y.

## Next bounded B7 wave

Admit exactly:

`B7_CANONICAL_SCENE_COMPOSITION_AND_GEOMETRY_V1 = ACTIVE / ADMITTED`

Exact next action:

`IMPLEMENT_B7_CANONICAL_SCENE_COMPOSITION_AND_GEOMETRY_V1`

This is a B7 structural composition wave. It is not B8.

The wave must treat these as one causal system:

1. stable canonical scene / table envelope;
2. table geometry invariant across states;
3. B-camera / stronger premium perspective;
4. wider but vertically foreshortened physical table;
5. lower far rail / more player breathing room;
6. preserve board/card readable size independently of physical felt scaling;
7. decouple feedback-dock surplus from table growth;
8. reclaimed feedback space becomes environment, hero foreground and breathing room;
9. reserve larger far-player volumes;
10. reserve deeper hero foreground zone;
11. scene-owned top coaching surface;
12. scene-attached bottom control shelf;
13. compact / canonical / large deterministic geometry proof.

## Required experiment gate

Before final geometry is committed, run a parameter-only rendered experiment:

- A = conservative / current-like;
- B = strong premium lift;
- C = maximum justified perspective.

Do not build three production implementations.

B is the current recommended target. Another direction may replace B only if
exact rendered evidence materially outperforms it while preserving learning
readability and the invariants in this authority.

## Poker-information readability guard

Premium perspective must not reduce learning readability.

Target guard:

- board-card readable size `>= ~95%` of current decision baseline;
- hero cards `>= ~95%` of current decision baseline;
- pot remains fully legible;
- commitment amounts remain at proven legibility;
- clue / board / pot retain clear separation.

If the camera requires more than approximately `5-8%` shrink of core poker
information, reject that camera direction.

## Player and hero space envelope

Reserve geometry approximately capable of:

Far players:

- `~76-88` logical px wide;
- `~88-108` logical px high.

Mid / side players:

- `~85-110` logical px wide;
- `~100-135` logical px visible height.

Hero foreground:

- `~250-300` logical px width;
- `~72-92` logical px visible depth.

These are design envelopes, not mandatory literal rectangles.

No production character pack is admitted in this wave. Procedural characters
or the existing experimental slice may be used only as temporary local visual
probes. Final assets must not be tuned to temporary geometry.

## Top and bottom surface disposition

`TOP_SURFACE_COHESION = IMPLEMENT_NOW_IN_SCENE_WAVE`

Target:

`SCENE_OWNED_COACHING_SURFACE`

Do not create another floating card.

`BOTTOM_SURFACE_COHESION = IMPLEMENT_NOW_IN_SCENE_WAVE`

Target:

`SCENE_ATTACHED_CONTROL_SHELF`

Dock height and content may change by state. Dock height must never resize the
physical table.

## Environment disposition

`ENVIRONMENT_TERRITORY = IMPLEMENT_NOW`

The current B2 environment receives more visible territory first.

`RICHER_ENVIRONMENT_ASSET_ART = EXPERIMENT_LATER / NOT_REQUIRED_IN_THIS_WAVE`

Do not start decorative room production in this wave.

## Deterministic geometry guard

Required viewports at one canonical text scale:

- `375x812`;
- `402x874`;
- `430x932`.

Drive the real learning loop through:

- decision;
- correct feedback;
- wrong feedback;
- repair;
- recheck;
- continuation / result.

For the same viewport, assert approximately `<= 0.5` logical px delta for:

- physical table rect;
- felt rect;
- table centerline;
- representative far-seat anchor;
- hero anchor;
- board anchor;
- pot anchor.

Also preserve:

- safe-area behavior;
- `>=44dp` tap targets;
- no-answer-scroll;
- CTA reachability.

## Preserved boundaries

Do not reopen:

- poker truth;
- evaluation;
- repair / recheck progression;
- content;
- clue semantics;
- B4 object ownership;
- B5 attention model;
- B6 semantic motion;
- card visual identity;
- dealer puck;
- telemetry semantics;
- `TEXT_SCALE_POLICY_V1 = SINGLE_CANONICAL_PRODUCT_SCALE`.

No final character production, P02, HNP reopening, or B8 work is admitted.

## Inherited reduced-motion debt

Unchanged:

- `INHERITED_REDUCED_MOTION_PROOF_BEAT_DEBT = NOT_B6_REGRESSION`
- `INHERITED_REDUCED_MOTION_PROOF_BEAT_DEBT = NOT_B6_BLOCKER`
- `INHERITED_REDUCED_MOTION_PROOF_BEAT_DEBT = MUST_RECEIVE_DISPOSITION_BEFORE_P02`
- `INHERITED_REDUCED_MOTION_PROOF_BEAT_DEBT = DEFER_TO_POST_B7_PRE_P02_MAINTENANCE`

## Canonical dispatch after this authority merges

`CURRENT_STAGE = VISUAL_GAUNTLET_B7_ACTIVE`

`B7 = ACTIVE_ADMITTED`

`B7_ITERATION_1_CODE_COHESION_V1 = CLOSED_PASS`

`B7_CHARACTER_VERTICAL_SLICE_V1 = CLOSED_PASS_PROOF`

`CHARACTER_ASSET_ARCHITECTURE = PROVEN`

`CLASS_LEVEL_LIFT = YES`

`PREMIUM_CHARACTER_ART = NOT_YET_PROVEN`

`ART_PRODUCTION_PIPELINE = REQUIRES_SEPARATE_SELECTION`

`FULL_CHARACTER_PACK = NOT_ADMITTED`

`TABLE_GEOMETRY_STABILITY = NOT_PASSED`

`TABLE_GEOMETRY_INVARIANT_V1 = REQUIRED`

`RECOMMENDED_CANONICAL_SCENE_DIRECTION = B_STRONG_PREMIUM_LIFT`

`B7_CANONICAL_SCENE_COMPOSITION_AND_GEOMETRY_V1 = ACTIVE_ADMITTED`

`EXACT_NEXT_ACTION = IMPLEMENT_B7_CANONICAL_SCENE_COMPOSITION_AND_GEOMETRY_V1`

`TEXT_SCALE_POLICY_V1 = SINGLE_CANONICAL_PRODUCT_SCALE`

`P02 = DEFERRED_NOT_STARTED`

`HNP_HARNESS = CLOSED_UNCHANGED`

`HUMAN_PROOF = FALSE`

`B8 = NOT_ADMITTED`
