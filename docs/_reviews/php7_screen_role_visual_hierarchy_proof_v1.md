---
status: "closed_proven"
status_source: "PHP-7 deterministic compact proof"
baseline: "b00d786af0ae84a1ac8530d93f078c8a1c5d83fa"
---

# PHP-7 Screen-Role & Visual-Hierarchy Proof v1

## Disposition

**`PHP7_CLOSED_PROVEN`**. The current Act0 Home and Learn shells already meet
the admitted compact-phone hierarchy without a product redesign. The red
`phone_first_premium_polish_v4_contract_test.dart` was a current-contract
carrier with stale literal references, not evidence of a learner-visible
hierarchy regression: the removed Learn support key and retired feedback copy
did not identify the current mission/feedback owners.

## Compact 360 x 640 proof

`./tools/screen_review_fast_v1.sh core compact` passed on the baseline above.
It deterministically captured the existing Home and Learn surfaces at the
compact device contract (360 x 640) alongside their supporting active screens.
The generated local evidence remains under `output/` and is deliberately not
committed.

The refreshed guard proves the visible emphasis contract through the current
owners:

| Surface | Highest-emphasis action | Competing information retained as support |
| --- | --- | --- |
| Home | `act0_shell_home_v6_primary_cta` | `act0_shell_home_quiet_sequence_surface` |
| Learn | `act0_shell_current_mission_cta` | `act0_shell_current_mission_card` and `act0_shell_current_mission_step_band` |

The CTA keys are rendered through the shared premium action button style; the
support keys retain route/step information without introducing a second action.
No copy, order, visual token, layout, asset, mascot, or Modern Table change was
needed.

## Six-surface role differentiation

The same active guard now names one distinct role element for every admitted
surface: Home route CTA, Learn mission CTA, Review repair CTA, Practice featured
action CTA, Profile recommended-focus card, and Session Summary victory lap.
This is source-level deterministic contract proof, not Human Novice evidence.

## Validation

- `flutter test test/guards/phone_first_premium_polish_v4_contract_test.dart` — passed (5 tests).
- `./tools/screen_review_fast_v1.sh core compact` — passed.

Human Novice Proof was not performed. SHK-CREST-01 remains outside this packet
and does not block this art-independent proof.
