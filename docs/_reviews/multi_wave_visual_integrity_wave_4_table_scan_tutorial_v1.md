---
status: "evidence_only__current_source_does_not_reproduce_wts_01"
status_source: "derived"
generated_by: "docs_frontmatter_v1"
---

# Multi-Wave Visual Integrity — Wave 4 Welcome Table-Scan Tutorial

## Verdict

`evidence_only__current_source_does_not_reproduce_wts_01`

## Source and current-evidence trace

The supplied WTS-01 screenshot identifies a tutorial state at `3/6` with the
copy `Carry the first table scan.` / `Real tables use the same simple scan.`.
Focused live-source search found no owner for either exact copy string. The
current Welcome owner (`act0_welcome_shell_v1.dart`) is a three-beat flow; the
approved compact current capture renders `Step 2/3`, not `3/6`.

The table-seat rendering seam would display `You`, `BTN Hero`, and a separate
BTN marker when a hero is also the dealer button. The available source proves
that the button marker has distinct poker meaning, but does not prove that the
missing six-step slide owns the same formatter or constraints.

## Classification

- Lower gap: `E — unresolved`; the named six-step route cannot be reproduced
  from current source/capture tooling.
- Identity labels: `You` is player identity; `BTN Hero` is the hero seat
  label; the separate BTN marker represents dealer/button position. Whether
  that exact redundancy exists on WTS-01 is unproven without its source owner.
- Progress: the current captured Welcome flow is independently three-step, so
  it cannot safely be treated as the WTS-01 `3/6` state.

## Fresh current evidence

- `output/evidence/multi_wave_visual_integrity_v1/wave_4_table_scan_tutorial/compact.welcome_decision.png`
- `output/evidence/multi_wave_visual_integrity_v1/wave_4_table_scan_tutorial/compact.welcome_handoff.png`

## Stop condition and next evidence

No implementation was admitted. Repairing the reported gap or labels would
require the exact six-step route/state owner (or a reproducible current route
that reaches it), plus a canonical 375 x 812 capture. The current screenshot
tool supports only the live three-beat Welcome states and was not modified.
