# Repository Reduction Decommission Ledger v1

Base HEAD: `0c922689cf41f3c31d22ce046bf16455d6fa66f7`

Columns: family/path | classification | active consumer | near-term planned owner | runtime/Graphify evidence | test evidence | Git-history evidence | action | files/bytes affected | validation required | reopen/recovery note.

## Executed actions

| Family/path | Classification | Active consumer | Near-term owner | Runtime evidence | Test evidence | Git-history evidence | Action | Files/bytes | Validation | Reopen/recovery |
|---|---|---|---|---|---|---|---|---|---|---|
| Root scripts: `fix_expected_paren.sh`, `test-analyze.sh` | safe_decommission | none | none | zero references anywhere; `fix_expected_paren.sh` reads `/tmp/targets.txt`/`/tmp/locations.txt` which don't exist, can't even run as committed | none | untouched since 2026-05-09 baseline snapshot | delete | 2 files | `flutter analyze`, `git diff --check` | recoverable via `git log -- <path>` |
| Root scripts: `diagnose_tests.sh`, `verify_and_log.sh` | safe_decommission | none | none | referenced only by `docs/canonical/PHASE_R_CLOSEOUT_AND_HANDOFF.md`, a closed-out historical phase doc; current canonical loop is `tools/fast_loop_world1_v1.sh`/`release_gate_world1.sh`/`checkpoint_world1_v1.sh` per AGENTS.md | none | untouched since baseline; superseded role confirmed in the closeout doc's own text | delete | 2 files | same | recoverable via Git history; `docs/canonical/PHASE_R_CLOSEOUT_AND_HANDOFF.md` left as historical record, not edited |
| `tools/*.dart`, `tools/*.sh`, `tools/*.py` orphaned set (478 files) | safe_decommission | none found | none found | scripted whole-repo reference check (basename search, excluding self) found zero hits outside each file itself for all 478; `tools/**` is excluded from `analysis_options.yaml` analyzer scope; confirmed no dynamic directory-walk invoker exists (the only 2 files that walk `tools/` dynamically, `documentation_ci_audit_pass.dart` and `ultimate_repo_audit.dart`, are themselves zero-referenced/orphaned) | none | many are sequential rewrites left in place (e.g. `automation_maintenance_loop.dart`/`_v2`/`_vFinal`; `visual_cohesion_audit`/`_v2`/`_v3`/`_final`/`_qa`/... ~18 variants); several are completed one-off audit/finalize passes (`global_monetization_qa_final.dart`, `ultimate_repo_audit.dart`, `ultimate_repo_consolidator.dart`, `quarantine_orphans.dart`) | delete | 478 files, ~3.6 MB | `flutter analyze` (tools/ already excluded, confirmed no change), `git diff --check` | recoverable via Git history; full 478-path list in this session's scratch log, available on request |
| `docs/_reviews/wave2_1_fix_landed_session_close_payoff_v1.md`, `wave2_2_premium_surface_hierarchy_v1.md`, `wave2_3_practice_confidence_first_week_content_v1.md` | archive_candidate | none | none | `docs/_reviews/wave2_4_beta_handoff_packet_v1.md` explicitly states "Waves 2.1, 2.2, and 2.3 are closed" | none | closure text confirms supersession | archive to `docs/archive/historical_audits/` (`git mv`) | 3 files | broken-reference scan (clean) | fully recoverable at new path; wave2_4 packet's closing-state description remains accurate |
| `docs/_reviews/claude_ux_v2_wave1_safe_now_cleanup_plan.md` | archive_candidate | none | none | superseded by `claude_ux_v2_wave1_closure_proof.md` (verdict `claude_ux_v2_wave1_closed_ready_for_wave2`, self-contained PR list) | none | plan-then-closure pair; closure doc doesn't need the plan doc's content | archive to `docs/archive/historical_audits/` | 1 file | same | recoverable at new path |
| `docs/_reviews/claude_ux_v2_wave2_scope_selection_v1.md` | archive_candidate | none | none | superseded by `claude_ux_v2_wave2_closure_recheck.md` (verdict `claude_ux_v2_wave2_closed_ready_for_wave3_decision`, self-contained PR list) | none | same pattern | archive to `docs/archive/historical_audits/` | 1 file | same | recoverable at new path |
| `docs/_reviews/top1_product_attack_plan_refresh_v1.md` | archive_candidate | one citation in `top1_product_attack_plan_deep_operating_map_refinement_v1.md` (fixed) | none | `top1_product_attack_plan_refresh_v2.md` itself states v1 "still treated Day 2 proof ... as the current frontier. That was stale after the repair-loop stack closed." | none | explicit v2 supersession text | archive to `docs/archive/historical_audits/`; updated the one citing doc to point at the new path and note the v2 supersession | 1 file + 1 reference edit | broken-reference scan (clean) | recoverable at new path |
| `output/playwright/` superseded capture-iteration families (act0_learn_v2-v6 manual probes; `controlled_demo` x5 non-canonical; `act0_capture_smoke` red/base/green1; `act0_learn_hierarchy_green1`; `act0_learn_ux_repair_green1`; `placement_welcome_wave1` original) | generated/reproducible, superseded | none | none | each family has a clear retained canonical/latest replacement (`act0_learn_final_premium_closeout_manual`, `controlled_demo_canonical_refresh_v1`, `act0_capture_smoke_green2`, `act0_learn_hierarchy_green2`, `act0_learn_ux_repair_green2`, `placement_welcome_wave1_fresh`); zero test coupling confirmed repo-wide (`grep test/ -r "output/"` finds only one unrelated string-literal assertion in a tool-source-text check, not a path read) | none read this evidence | commit-date sequencing plus explicit naming (`_final`, `canonical_refresh`, `green2`, `_fresh`) shows iteration order | delete (screenshots are reproducible capture artifacts, not unique textual evidence) | 385 files | broken-reference scan (clean, zero test/doc references) | not recoverable from disk but fully recoverable via Git history if a specific iteration is ever needed again |

## Evaluated, not touched (with reason)

| Family/path | Classification | Reason retained |
|---|---|---|
| `lib/ui_v2/persona/*` | N/A (doc/reality mismatch) | Path named in `PROJECT_TOPOLOGY_AND_TRUTH_MAP_v1.md` as dormant does not exist anywhere in the repo. Nothing to decommission; flagged for the topology doc's owner. |
| `lib/ui_v2/ai_coach/*` (3 files, ~837 lines) | uncertain_do_not_touch | Unreachable from Act0, but has live non-Act0 service consumers (`ai_coach_service.dart`, `ai_coaching_analytics_service.dart`) and direct smoke-test coverage (`test/smoke/ai_coach_overlay_smoke_test.dart`, `ai_coaching_analytics_smoke_test.dart`) that import real classes. |
| `lib/personalization/*` (21 files, ~2,719 lines) | active_support | Powers the live (non-Act0) `session_result_screen.dart` (14 imports) and `universal_intake_plan_screen.dart` (8 imports) flow; ~24 test files directly import real classes. Topology doc's "dormant" framing does not match current reachability. |
| `lib/ui_v3/*` (1 file, `visual_theme_v3.dart`, 63 lines) | active_support | Load-bearing stub, directly imported by ~14 live `ui_v2/table` widgets (`table_surface.dart`, `board_surface.dart`, `bet_slider.dart`, chip widgets, `personalization_hub.dart`). Deleting it breaks the build. A separate, pre-existing, unrelated broken-import issue was noted (some files reference `ui_v3/theme/adaptive_theme_bridge.dart` etc. that do not exist) — flagged for the owner, not fixed here (out of scope, pre-existing, not a decommission target). |
| `lib/ui_v2/screens/drill_runner_screen.dart` (1,239 lines) | uncertain_do_not_touch | Zero production reachability confirmed (no `lib/` file constructs `DrillRunnerScreen(`). However 14 test files reference it, and several — `world1_flow_e2e_contract_test.dart`, `world1_readiness_smoke_contract_test.dart`, `world1_table_practice_gating_contract_test.dart`, `drill_table_first_presence_contract_test.dart` — are broad multi-screen E2E/readiness gates with conditional branches (legacy vs. canonical table-practice path), not narrow single-purpose tests of the dead widget alone. Two guard tests (`world1_mvp_ship_block_contract_test.dart`, `world1_spine_10_hand_contract_test.dart`) assert its *absence* in real app boot and would need editing, not deletion, if the file were removed. Disentangling this safely requires per-test-file judgment beyond this bounded pass's confidence bar; genuinely dead production code, but too entangled with release-gate test surface to touch here. |
| `lib/ui_v2/table/table_surface.dart` (38 lines) | active_support | Imported by `module_summary_screen.dart` and `world1_surfaced_table_section_composer_v1.dart`, both reachable via the live (non-Act0) `table_first_navigation.dart` → `module_launcher_screen.dart` chain. |
| `lib/ui_v2/map/ui_v2_progress_map_screen_v2.dart` | N/A (already relocated) | Does not exist under `lib/`; the real file already lives at `docs/archive/archived_old_versions/ui_v2_progress_map_screen_v2.dart`, outside the compiled app. Nothing to remove. A pre-existing, unrelated guard test (`test/guards/legacy_training_launcher_boundary_contract_test.dart`) still hardcodes the old `lib/` path and fails with `PathNotFoundException` — confirmed failing on the unmodified base branch, not caused by this task. |
| `lib/archive/legacy_runners/*` (8 files, ~25,706 lines) | active_support | Despite the topology doc's label ("no longer active runtime surfaces"), this family is still live-wired into the non-Act0 legacy home/training flow via `table_first_navigation.dart` → `pushWorld1FoundationsRunnerV1`, called from `module_launcher_screen.dart` and `module_summary_screen.dart`. 3 test files import it directly; several string-based boundary guards assert on it. **This is a significant discrepancy between the topology doc's claim and actual reachability — flagged for the owner, doc not edited in this pass.** |
| `TrainingPackPlayScreen` / `TrainingSessionScreen` compatibility family | active_support | Heavily used, intentional compatibility bridge: `TrainingPackPlayScreen` has 27+ production references; `TrainingSessionScreen` is instantiated via `lib/services/canonical_legacy_training_launch_v1.dart`, guarded by 3 dedicated contract tests plus 16+ additional test files. |
| `lib/ui_v2/ui_v2_beta_shell.dart` (`UiV2BetaShell`, 451 lines) | uncertain_do_not_touch (flagged for future pass) | No import/instantiation found anywhere in `lib/` or `test/` (the only name hits are an unrelated same-named prototype class under `design/components/`, and a descriptive mention in `docs/audit/investigations/topology_entry_seam_audit_v1.md`, which treats its internals as a live investigation subject for centralizing world-pack-order logic into canonical services). Genuinely dead as a mounted widget, but the open investigation doc creates near-term-planned ambiguity; not decommissioned in this bounded pass. Recommend a dedicated follow-up task. |
| `lib/tools/v4_persona_mat_consistency_qa.dart`, `v4_visual_polish_final.dart`, `visual_cohesion_v4_qa.dart`, `visual_tokens_v4_verifier.dart` | uncertain_do_not_touch (flagged for future pass) | Discovered while cross-checking the `tools/` decommission (they share basenames with removed root-level `tools/` files but are structurally unrelated, self-contained). Each is a self-referential no-op stub (imports `package:poker_analyzer/tools/<self>.dart` and calls its own trivially-returns-0 function). Already excluded from analyzer scope (`lib/tools/**`). Not investigated further in this pass; likely a dead scaffold family worth a dedicated look. |
| `docs/plan/ROUTE_TO_B_EXECUTION_RESET_v1.md`, `ROUTE_TO_B_ACTION_LADDER_v1.md` | active_support (do not archive) | AGENTS.md calls these "archived execution docs," but `test/tools/beta_ship_runbook_v1_test.dart` and `test/tools/release_readiness_calibration_v1_test.dart` treat them as active, asserting on live content (e.g. "Current Active Block"). Archiving would break those tests further. Left in place; AGENTS.md's characterization vs. test reality is a discrepancy worth the owner's attention, not resolved here. |
| `docs/plan/MASTER_PLAN_v2.2.md` | historical_unique (working as designed) | Already a self-declared "HISTORICAL STUB / DO NOT USE" redirect pointing to `MASTER_PLAN_v3.0.md`; intentionally kept as a compatibility pointer, referenced by the same two test files above. |
| `docs/_reviews/w11_route_proof_goal_pack_v1.md` | historical_unique | Initially a naming-convention supersession candidate (v1 -> v2), but v1's verdict (`blocked_projection_to_route_shape_gap`) documents a *distinct blocked approach*, not an earlier draft of v2's solution. Keeping it prevents a future agent from re-attempting the same blocked `MicroTaskStep`-mapping approach. Kept. |
| Certification-closure ladder (`w2_8_0_certification_closure_v1.md`, `w3_bounded_8_0_certification_closure_v1.md`, `w4_w5_bounded_certification_closure_v1.md`, `w6_bounded_certification_closure_v1.md`) | historical_unique | Pattern-matched as a possible supersession chain but are closures of *different* W-scopes, not duplicates of each other. Not touched. |
| `w1_w6_*` side-audit cluster (`_first_week_content_trust_pass_v1`, `_learning_outcome_independent_audit_v1`, `_migration_coverage_consolidation_v1`, `_outcome_repair_verification_local_cleanup_v1`, `_prerequisite_chain_repair_batch_v1`, `_schema_migration_pilot_v1`) | uncertain_do_not_touch | Not read in this pass; distinct from the confirmed-active w1-w6 final-audit/grouped-repair-program/wave-closure chain. No supersession evidence gathered; left untouched pending a dedicated read. |
| `docs/_reviews/` (630 files after archiving) and `docs/archive/` (478 files after archiving) bulk | historical_unique / archive_candidate (not bulk-processed) | An md5 duplicate scan across all of `docs/` (from the prior hygiene pass) found zero exact duplicates. This pass found and acted on 6 well-evidenced supersession candidates via explicit textual confirmation; a full per-file classification of the remaining ~1,100 files was out of scope for one bounded wave (`Do not retain 635 reviews merely because each file has a different checksum` was honored for the candidates actually inspected, not applied as a blanket bulk action without evidence). |
| Root wrapper `run_table_first_tiers.sh` | uncertain_do_not_touch (low-value-keep) | 3-line forwarder to `tools/run_table_first_tiers.sh`. CI calls the `tools/` version directly, never this wrapper — genuinely unused by automation, but trivial (3 lines) and harmless as a human convenience entrypoint. Not worth the removal for the size of the win; left in place. |
| Tracked `output/` non-superseded families (`act0_profile_hierarchy_green1`, `act0_practice_hierarchy_green1`, `profile_wave2_packet`/`profile_wave2_live`, `runner_phase1`/`runner_phase2`, `learn_430_focus`, `learn_micro_pass`, `act0_tooling_debug_run1`, `act0_simulator_proof_sweep_v1`, `act0_learn_ux_repair_direct1`, `act0_learn_v2_current_mission_abs`, `device_audit/*`, `simulator/*`) | canonical_evidence / uncertain_do_not_touch | No version-sibling exists to prove supersession (single-capture families), or (for `act0_learn_ux_repair_direct1`) the "latest wins" signal was ambiguous (parallel alternate-approach probe, not a clean iteration of `green2`). Left untouched. |

## Pre-existing issues discovered (not caused by this task, not fixed — flagged for owner)

| File | Issue | Evidence it predates this task |
|---|---|---|
| `test/tools/release_readiness_calibration_v1_test.dart` | Hardcodes `docs/README_SSOT.md`, which does not exist anywhere in the repo | Fails identically with working tree fully stashed back to base HEAD `0c922689` |
| `test/tools/beta_ship_runbook_v1_test.dart` | Hardcodes `docs/plan/TRUE_RELEASE_READINESS_SSOT_v1.md`, which was already relocated to `docs/archive/obsolete_plans/` before this task | Same stash-based confirmation |
| `test/guards/legacy_training_launcher_boundary_contract_test.dart` | Hardcodes `lib/ui_v2/map/ui_v2_progress_map_screen_v2.dart`, which already lives at `docs/archive/archived_old_versions/` | Same |
| `test/guards/world1_foundations_microtask_contract_test.dart` | References undefined symbols `kWorld1RunnerModeCampaignSpine` and constructor `World1FoundationsMicroTaskRunnerScreen` not resolvable from its current imports | Fails identically with working tree fully stashed back to base HEAD |
| `test/guards/world1_canonical_terminal_surface_cutover_contract_test.dart`, `test/ui_v2/runner/world1_canonical_runner_authority_v1_test.dart`, `test/ui_v2/runner/world1_canonical_table_render_branch_v1_test.dart` | Hardcode `lib/ui_v2/runner/world1_foundations_microtask_runner_surface_v1.dart` and `lib/ui_v2/screens/modern_table_screen_v1.dart`, neither of which exists at those paths (the real file lives under `lib/archive/legacy_runners/`) | Zero `lib/` files were touched by this task (confirmed via `git diff --cached --name-only`); paths were already absent |

This task made **zero changes under `lib/` or `test/`**, so none of the above are regressions introduced here.
