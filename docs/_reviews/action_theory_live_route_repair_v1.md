---
status: "PUBLISHED FOR ADMISSION"
status_source: "derived"
baseline: "5e2727b0101d"
generated_by: "docs_frontmatter_v1"
---

# Action theory live-route repair v1

Status: PUBLISHED FOR ADMISSION

## Canonical candidate and bounded scope

- Candidate: `5e2727b0101d433708198c598ae38381cef8da12` on
  `codex/action-theory-live-route-repair-v1`.
- Baseline: `ce7d146265f4cc6c8b855886f8e7e78fd19872e5`.
- Scope is the Action theory-to-decision continuity, Action error authority,
  stable-practice feedback-card identity, and focused route coverage. No
  visual, copy, Modern Table, Theory CI, or product expansion work is included.

## Repairs

1. The `actions_theory` task now hands off through the Action registry to
   `actions_check_drill`, while persisting the canonical decision target.
2. The stable-practice review surface now carries the canonical
   `act0_shell_feedback_card` identity. Parent-owned review state was already
   correct; its child feedback surface was not identifiable by the route
   contract.
3. Active Action outcome telemetry now uses `misread_action_legality`.
   `missed_action_read` remains legacy read/persistence compatibility only;
   no active write in the verified route emits it.

## Factory authority

The completed Factory package is
`output/review_bundles/canonical_journey_truth_progression_v1_5e2727b0`.
Its durable Factory carrier exited `0` (`output/durable_factory_5e2727b0/exit_code.txt`).

| Artifact | SHA-256 |
| --- | --- |
| `10_manifest/manifest.json` | `5f48d6646a029b29dea42f2a6042e64dbbb6c663cc33cf1de13196f3cc6f09f7` |
| `09_validation/command.log` | `ce91cf96fd7eccf028e37db49cd3684ac014e07a6e89c534d2041d9555d301e1` |
| ZIP (8,562,470 bytes) | `b80eeb37aa8411848ce465e465e170769da70f07f88200a7d31112ff814bef2d` |

The Factory trace is
`08_telemetry/unlocked_action_wrong_repair_recheck_trace.json` (49 events).
It contains canonical `misread_action_legality` emissions and zero active
`missed_action_read` emissions. Its command log records the deterministic
validation, analysis, fast loop, Graphify hook, diff checks, and Action
admission pass.

## Xcode build-control recovery

After isolating only the candidate `build/ios` state and candidate DerivedData,
the controlled detached build completed successfully:

```text
flutter build ios --simulator --debug --dart-define=HNP_TELEMETRY=true -v
start: 2026-07-20T16:31:31Z
end:   2026-07-20T16:39:51Z
exit:  0
```

The fresh product was
`build/ios/iphonesimulator/Runner.app`; executable
`build/ios/iphonesimulator/Runner.app/Runner` has SHA-256
`2224f19c82542883c95210405161737b71f451b1304519e1a7bf3cca0a5eb612`.
Bundle identifier: `com.example.pokerAnalyzer`.

The prior app was terminated and uninstalled, removal of its old container was
confirmed, and the fresh bundle was installed and launched on iPhone 16 Pro
Simulator. Flutter-generated macOS registrant drift was restored after build;
the tracked worktree and index were clean before this evidence document.

## Installed GUI proof

Profile B used the production-equivalent persisted precondition: four complete
prerequisite lessons and all 28 prerequisite tasks. The normal product surface
showed `4 of 9 lessons complete` and Action as `Now`; no debug menu or
direct-state route was used for the walkthrough.

The installed route completed in order:

1. Learn opened Action at `actions_theory`.
2. Visible theory continuation reached `actions_check_drill`.
3. Wrong `Fold` produced causal feedback and same-signal repair.
4. Repair answered `Check`, then the original-source recheck answered `Check`.
5. The recheck showed the recovered recommendation; the trace records
   `recoveredSuccess` payoff and `recoveredAfterRepair` outcome.
6. The route exited back to Learn explicitly.

Runtime identities are `actions_theory` (theory), `actions_check_drill`
(source decision, repair, and recheck), and
`w1_action_words_no_bet_read_v1` (canonical Action context identity).
Ordered installed screenshots are local-only under
`output/xcode_build_control_5e2727b0/live_gui/`.

## Physical HNP JSONL and privacy

Physical source:
`Library/Application Support/act0_hnp_trace_v1.jsonl` in the freshly-installed
Simulator container. The uncommitted copied evidence is
`output/xcode_build_control_5e2727b0/act0_hnp_trace_v1.jsonl`.

- SHA-256: `230c25f93ca6cb6df366fea48e731bd0c9dae657ef4f5746834856938f6ecca3`
- Events: 49; every JSONL line parses and the final line is newline-terminated.
- Lifecycle: theory -> wrong source choice -> causal feedback -> repair ->
  correct repair -> original-source recheck -> correct recheck -> recovered
  payoff/recommendation -> explicit `session_exited`.
- One repair entry, one recheck entry, one recovered payoff, and exactly one
  `session_exited` were recorded. All active non-`none` Action errors are
  `misread_action_legality`; active legacy count is zero.
- Decision buckets are bounded (`3_to_10s` or `unknown`); exact decision
  milliseconds are absent. `board_card_ids`, `tableSignal`, `missedSignal`,
  learner-facing copy/context, identity, and network fields are absent.

The trace uses only canonical route identifiers and local session correlation;
it contains no raw board-card payload, learner-facing feedback copy, account,
or network data. Raw JSONL remains untracked and is not committed.

## Changed-file matrix

| Area | Files |
| --- | --- |
| Action sequence and personalization | `act0_action_learning_sequence_v1.dart`, `act0_action_sequence_personalization_v1.dart`, `act0_action_session_payoff_v1.dart` |
| Route/feedback/telemetry owners | `act0_lesson_runner_shell_v1.dart`, `act0_shell_preview_screen_v1.dart` |
| Focused contracts | `act0_action_learning_sequence_v1_test.dart`, `act0_telemetry_sink_v1_test.dart`, `alpha_journey_progression_truth_v1_test.dart` |

## Deferred debt and non-claims

Visual spacing, copy, Modern Table, Theory CI, tablet work, and broader
telemetry redesign remain deferred. This record does not claim fresh-install
Action access, human-novice efficacy, release readiness, remote analytics, or
production release admission. Profile A remains gated at the first lesson;
this proof is only the production-equivalent unlocked Profile B route.
