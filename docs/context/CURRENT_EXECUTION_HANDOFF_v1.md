# Current Execution Handoff v1

Status: CURRENT-STATE CONTINUATION AID / NOT PRODUCT AUTHORITY

Master Plan v4 wins on product direction.
`PRE_HUMAN_CAMPAIGN_STATE_v1.md` wins on exact campaign dispatch where
applicable. This file only compresses current state for low-context continuation.

## CURRENT EXACT MAIN

Re-resolved before the integrated 2.5D post-admission authority reconciliation:
`c0263f448b982af9f5066b431a741ebc79b80b4a`.

Consumers must re-resolve live `origin/main` before execution; this handoff does
not make its authoring SHA a permanent authority.

## CURRENT NORTH STAR

Product / brand:

> The poker coach that turns mistakes into corrected table instincts.

Operational:

> Turn a learner's poker mistake into a corrected decision that transfers to a
> new table spot and still holds later.

Canonical authority:
`docs/plan/MASTER_PLAN_v4_NORTH_STAR_INTEGRATED.md`.

## CURRENT STAGE

`MACHINE_NORTH_STAR_CONVERGENCE`

Purpose: exhaust confirmed high-EV machine-reducible North Star gaps to the
point of diminishing return before spending real Human evidence, without
speculative feature or polish accumulation.

Preserved status:

- `PRE_HUMAN_READY = MET`
- `VISUAL_BASELINE_ACCEPTED_FOR_HNP`
- `VISUAL_NORTH_STAR_GAP_OPEN`
- `HUMAN_PROVEN_10_OF_10_CANDIDATE = NOT_MET`
- `HNP = REQUIRED_FUTURE_HUMAN_EVIDENCE_GATE / NOT_PERFORMED`
- Modern Table = `MAINTENANCE_MODE`

HNP is not cancelled. The project is resolving one admitted high-EV
machine-reducible visual architecture question before the diminishing-return
gate.

## MACHINE-PROVEN / HUMAN-UNPROVEN BOUNDARY

Machine-proven mechanisms remain substantial across Act0 progression, poker
truth, decision/feedback/repair/recheck/payoff, transfer/retention machinery,
bounded telemetry, and source-owned personalization. Wave 3 machine/native
acceptance remains closed at product evidence head
`486a1509748a556de11a7e90076ed6e53aefa06e`.

Still unproven by machine evidence:

- real novice first-session understanding/value;
- real learner transfer and automaticity;
- Day-2 / Day-7 retention outcome;
- personalization outcome lift;
- subjective Human trust/desirability where Human evidence is required;
- real-poker improvement;
- market demand / willingness to pay;
- global `10/10` / `#1` claim.

## LEARNING SCENE OLD B - CLOSED

`LEARNING_SCENE_RESTRAINED_DEPTH_CODEX_EXECUTION_V1_CLOSED_CONTROL_RETAINED`

The old admitted overlap-based experiment was actually executed locally on
exact main `c0263f448b982af9f5066b431a741ebc79b80b4a`; it was not a procedural
stop.

Tested mechanism only:

- lower foreground side wings;
- approximately `18px` relationship;
- `136px` center opening;
- rounded inner top corners;
- existing lower-dock material;
- table allocation unchanged;
- lower teaching allocation unchanged.

Result:

- visually positive;
- failed the then-current hard `1.4x` geometry contract;
- feedback block offset became `84.5` versus prior permitted `<= 66.08`;
- pristine exact-main control PASS;
- candidate B FAIL;
- reverted control PASS;
- maximum two repair iterations exhausted;
- candidate fully reverted;
- no production mutation;
- no branch/commit/PR/merge from the experiment.

Final B disposition:

`CLOSED / CONTROL_RETAINED / REVERTED / FALSIFIED_FOR_THIS_BOUNDED_OVERLAP_MECHANISM`

Do not call B visually ineffective. Do not reopen B2/B3.

Critically, old B did **not** test:

- prompt above table;
- perspective table geometry;
- near/mid/far seat hierarchy;
- explicit hero foreground;
- hero/action spatial ownership;
- object-attached integrated scene;
- same-scene 2.5D composition.

Therefore old B closure does not falsify the newly admitted architecture.

## INTEGRATED 2.5D PROTOTYPE ADMISSION

Completed read-only audit:

`SHARKY_INTEGRATED_2_5D_LEARNING_SCENE_PROTOTYPE_ADMISSION_V1`

Terminal verdict:

`SHARKY_INTEGRATED_2_5D_LEARNING_SCENE_PROTOTYPE_ADMITTED`

Admitted candidate:

`SHARKY_INTEGRATED_2_5D_LEARNING_SCENE_V1 = ADMITTED / NOT_IMPLEMENTED / UNPROVEN`

Concept:

`PROMPT -> RESTRAINED 2.5D TABLE EVIDENCE -> HERO FOREGROUND -> ACTION`

The intended product read is one integrated learning scene, not a prompt plus a
flat table widget plus detached controls.

Admission rationale, without converting admission into a success claim:

- the open visual North Star gap has a structural component;
- moving only the prompt above the current flat table leaves a material spatial
  gap unresolved;
- the spatial gain can be tested without genuine 3D;
- existing Act0 poker semantics can remain unchanged;
- no second poker/strategy engine is needed;
- implementation can be bounded and reversible;
- Modern Table need not be broadly reopened;
- contextual answer-bearing coaching is not required;
- existing deterministic evidence tooling is sufficient;
- expected engineering cost is proportionate to product information gain.

## POKERSKILL BENCHMARK ROLE

Owner-supplied independent forensic evidence found:

`POKERSKILL_ADVANTAGE_PRIMARILY_SPATIAL_SCENE_ARCHITECTURE`

with secondary contributions from information architecture, object-attached HUD,
and hero/action spatial ownership, and strong evidence of a Flutter 2.5D hybrid
rather than genuine 3D.

This is hypothesis evidence only. PokerSkill is not Sharky product authority.
Do not copy proprietary implementation, assets, art, scene documents, or exact
layouts.

## ADMITTED PROTOTYPE SCOPE

Minimum causal mechanisms only:

1. perspective table geometry;
2. explicit near/mid/far seat presentation tiers;
3. hero foreground ownership;
4. neutral prompt above table;
5. hero-owned action foreground;
6. minimal preservation/extension of object-attached HUD;
7. same-scene decision -> repair -> recheck continuity.

Single purpose: test whether this structure materially increases Sharky's
Learning Scene visual/product ceiling while preserving deterministic learning
semantics.

Explicit non-scope:

- real 3D / Unity / new rendering engine;
- scene JSON runtime or generalized world-coordinate engine;
- depth/lighting maps, fragment shaders, environmental room system;
- illustrated opponents, camera animation, parallax, gyro;
- new motion framework, multiple themes/rooms, new art pipeline;
- broad content changes, AI/ML, contextual pre-answer coaching;
- full-course migration.

## POKER / LEARNING SEMANTIC INVARIANTS

Preserve:

- `Act0RunnerStateV1` as runner/progression truth;
- `Act0TableStateV1` as canonical poker/table truth;
- existing task IDs and option IDs;
- existing correctness and poker strategy truth;
- existing repair and recheck semantics;
- existing telemetry;
- existing content.

Presentation may change only geometry, scale, perspective, z-order, visual
attachment, and composition. No duplicate poker logic.

## CONTEXTUAL COACHING STATUS

Old Candidate C = perspective/depth + contextual coaching.

Status: `NOT_ADMITTED`.

The newly admitted architecture-audit Option C is not old Candidate C. The new
candidate is spatial architecture only. Neutral prompt is allowed; answer-
bearing attention guidance remains outside scope.

## MODERN TABLE BOUNDARY

Modern Table remains `MAINTENANCE_MODE`.

The prototype is admitted as a Learning Scene presentation/composition
experiment using existing poker state and reusable visual atoms. It is not
authorization for standalone Modern Table polish, cosmetic reopening, a fourth
cohesion wave, or broad production table redesign.

Existing canonical Modern Table/default table presentation remains control.

## OWNER PRIMARY VISUAL PRIORITY

Primary architecture target for this phase:

- one excellent premium modern portrait-phone experience;
- standard/default text scale;
- clear prompt;
- clear poker evidence;
- immediate hero orientation;
- clear actions;
- correct safe areas and hit targets;
- no critical clipping;
- no semantic regression.

Do not select the primary architecture around preservation of historical `1.4x`
positional/pixel geometry.

## ACCESSIBILITY POLICY FOR PROTOTYPE

`1.0x` standard text = PRIMARY HARD VISUAL ACCEPTANCE TARGET.

Existing `1.4x` may be inspected as a secondary catastrophic-usability signal
when cheap. `1.4x` must not veto merely because spacing changes, geometry shifts,
an old offset threshold is exceeded, or enlarged-text composition is less
polished than primary `1.0x`.

Still reject catastrophic failure:

- hidden required actions;
- unusable hit targets;
- severe clipping that makes the task impossible;
- broken navigation;
- wrong poker ownership;
- safe-area blocker.

Do not disable Dynamic Type or global text scaling, delete semantics or
accessibility APIs, or deliberately create unusable enlarged-text flow. Full
accessibility optimization is deferred until the primary Learning Scene
architecture is proven. A later canonical production migration must explicitly
reconcile obsolete geometry guards.

## CHEAPEST FALSIFIER AND DEVICES

Sequence:

`w1_action_words_check_v1`

Task family:

`actions_check_drill`

States:

1. decision;
2. wrong / repair-facing;
3. recheck / recovered outcome.

No fourth state unless implementation proves it necessary.

Device policy:

- primary: `iphone17_class`, `402x874`, text scale `1.0`;
- secondary: `compact`, `375x812`;
- secondary: `large_phone`, `430x932`.

No broader first-prototype matrix.

## OWNER-FACING SUCCESS QUESTION

Core question:

Does the candidate change perception from:

`I am answering questions below a poker-table widget`

to:

`I am learning by making decisions from my seat at the table`?

Supporting checks:

- hero position is immediate;
- prompt establishes the task before table reading;
- gaze flows Prompt -> Evidence -> Hero -> Action;
- table feels spatial rather than diagrammatic;
- near/mid/far roles read clearly;
- hero cards/actions feel like one system;
- HUD remains attached to poker owners;
- feedback preserves spatial place;
- candidate is materially more premium than control;
- poker truth is unchanged;
- answer leakage is unchanged.

Do not replace these with arbitrary geometry proxies.

## TECHNICAL HARD FAILURES

- incorrect poker state;
- wrong hero or position;
- card/seat mismatch;
- chip/bet ownership mismatch;
- visual/hit-target mismatch;
- hidden required action;
- primary-profile critical clipping;
- safe-area blocker;
- broken decision -> repair -> recheck flow;
- unstable deterministic evidence;
- answer leakage;
- duplicate poker logic;
- new 3D dependency;
- uncontrolled Modern Table production mutation.

## ITERATION BUDGET

`INITIAL PROTOTYPE + MAXIMUM TWO BOUNDED GEOMETRY REPAIRS`

Permitted repair dimensions:

- perspective strength;
- seat depth anchors/scales;
- hero/table/action relationship;
- prompt/table vertical budget;
- collisions/hit geometry.

After repair #2: STOP and return control/candidate evidence to the product owner.
No endless machine visual-polish loop.

## EXECUTOR

Exact implementation executor after this reconciliation:

`Codex + local Flutter + existing deterministic capture tooling`

Native/iOS Simulator is not required for the first causal falsifier. Image
generation/art work and a new dependency are not required. If a candidate later
wins owner validation and is proposed for production, native proof may be
separately required.

## HNP TECHNICAL STATUS

- `HNP = REQUIRED_FUTURE_HUMAN_EVIDENCE_GATE / NOT_PERFORMED`
- `HNP_NATIVE_BUILD = PASS`
- `HNP_PROFILE_A = PASS`
- `HNP_PROFILE_B_NATIVE_MATERIALIZATION = BLOCKED / DEFERRED`
- `HNP_PHYSICAL_TELEMETRY_FULL_CHAIN = NOT_YET_PROVEN`
- `REAL_HUMAN_HNP = NOT_PERFORMED`

Known blocker: canonical Profile B persisted prerequisite state is overwritten
by fresh progress during app bootstrap; no supported installed-Simulator
materialization operation is currently validated. Reopen immediately before
Human HNP unless another admitted Gauntlet naturally resolves it.

## PROTECTED / DO-NOT-REOPEN SURFACES

- Modern Table remains Maintenance Mode;
- no fourth visual cohesion wave;
- no unrelated visual micro-polish;
- no speculative guards or tests-for-tests;
- no generic AI/ML architecture or dormant Persona/AI Coach/ui_v3 resurrection;
- no W13-W36 bulk production, full solver, broad refactor, or commerce-before-
  evidence;
- Human evidence classes cannot be synthetically promoted.

## CLOSED OPERATIONAL CAPABILITY

`PR_VISUAL_EVIDENCE_INNER_LOOP_V1 = CLOSED / IMPLEMENTED`

Final implementation disposition:
`PR_VISUAL_EVIDENCE_BEST_EFFORT_DISPOSITION_V1_CLOSED`.

This closes the operational multiplier only. It does not close
`VISUAL_NORTH_STAR_GAP_OPEN`.

## EXACT NEXT ACTION

`SHARKY_INTEGRATED_2_5D_LEARNING_SCENE_CODEX_PROTOTYPE_EXECUTION_V1`

Purpose: build one reversible, production-fidelity, candidate-only
control-vs-2.5D prototype on existing Sharky semantics.

Do not broaden into generic visual polish, Modern Table production redesign,
contextual coaching, poker semantic changes, answer-bearing cues, new
dependencies, real 3D, or full-course migration.

Machine convergence exits when all confirmed high-EV machine-reducible material
gaps are closed, boundedly deferred, or diminishing-return; no machine-obvious
P0/P1 remains; the major uncertainty is intrinsically Human/longitudinal/
real-poker/market-bound; and evidence tooling is sufficient to react cheaply to
Human findings.

`HANDOFF_AUTHORITY = NONE`
`MASTER_PLAN_V4_WINS_ON_PRODUCT_DIRECTION = TRUE`
`EXACT_NEXT_ACTION = SHARKY_INTEGRATED_2_5D_LEARNING_SCENE_CODEX_PROTOTYPE_EXECUTION_V1`
