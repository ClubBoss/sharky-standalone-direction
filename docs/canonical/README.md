# Canonical Documents

> REFERENCE/GOVERNANCE ONLY (Phase 6-8 artifacts). The active SSOT chain is defined in `docs/README_SSOT.md`.

This folder preserves the canonical governance/reference artifacts for Master Plan 6.x activities (Phase 6–8).
Use these for historical decisions, audits, and phase traceability.
No document content was modified while relocating files.

## Canonical entrypoint
- `phase_6/` -> Phase 6 SSOTs (Design Lock, Visual Lift, UX Polish, Content Design Spec).
- `phase_7/` -> Phase 7 governance (content expansion execution + completion lock).
- `phase_8/` -> Phase 8 framing, evidence readiness, stress test, and decision gate.

For current active product/content planning SSOTs, return to `docs/README_SSOT.md`.
