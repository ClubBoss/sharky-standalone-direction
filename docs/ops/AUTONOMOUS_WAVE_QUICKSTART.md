# Autonomous Wave Quickstart

Use:
- `docs/ops/AUTONOMOUS_WAVE_PROTOCOL_SSOT.md`

Execution intent:
- discover latest canonical Audit Hub truth
- execute exactly one bounded cycle
- adapt wave type to current project state
- return the standard 10-block summary only
- do not ask clarifying questions unless the protocol says STOP

Standard launch command:

Run `docs/ops/AUTONOMOUS_WAVE_PROTOCOL_SSOT.md`.
Use the latest canonical Audit Hub truth and execute exactly one bounded cycle.
Return the standard 10-block summary only.