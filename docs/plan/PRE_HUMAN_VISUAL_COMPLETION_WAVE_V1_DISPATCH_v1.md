# Pre-Human Visual Completion Wave V1 Dispatch v1

Status: **ACTIVE — Wave V1 of the visual completion corridor.** Authorizes
Wave V1 only. Does not authorize Wave V2 or Wave V3.

Authority: does not override `docs/context/PRE_HUMAN_CAMPAIGN_STATE_v1.md`
(single first-read dispatch authority) or `docs/plan/MASTER_PLAN_v3.0.md`. It
reconciles the campaign state's pending baseline field and backfills the
Top-3 decision record the strategy checkpoint omitted (see below). It does
not authorize product implementation beyond Wave V1's own bounded diagnostic
scope, broad redesign, Modern Table changes, new Sharky art, PHP-9, or Human
Novice Proof.

## 1. Baseline reconciliation

| Field | Value |
| --- | --- |
| Canonical HEAD | `a27366e6f6f4a25e6862a053945be6a5f4a421cd` (PR #87 merge — resolves the "pending" placeholder left in `PRE_HUMAN_CAMPAIGN_STATE_v1.md`) |
| Latest admitted PR | **#87** — `docs/plan/PRE_HUMAN_VISUAL_COMPLETION_STRATEGY_v1.md` Publication 2 (terminal) |
| Prior PR | #86 (`432c9f47e65cd79ed1eb113a952d9fc077468f15`) — VS-0 |
| Strategy checkpoint | `PRE_HUMAN_VISUAL_COMPLETION_STRATEGY_COMPLETED` (unchanged, terminal) |
| Active packet | **Wave V1 of the visual completion corridor** (`docs/plan/PRE_HUMAN_VISUAL_COMPLETION_STRATEGY_v1.md` §13) |
| Autonomous window | **OPEN for Wave V1 only**, per this dispatch. Does not pre-open Wave V2 or Wave V3 — each remains evidence-conditional on the prior wave per strategy §13. |
| PHP-9 | **NOT_PREAUTHORIZED** (unchanged) |
| Human Novice Proof | **NOT AUTHORIZED** (unchanged) |
| Modern Table | **Maintenance Mode** (unchanged, not touched by Wave V1) |
| VISUAL_SYSTEM_COMPLETENESS | **NOT CLOSED** (unchanged — Wave V1's full matrix is required before this can move) |
| SHK-CREST-01 | **Unresolved** (unchanged, not reopened) |
| No new Sharky art | Confirmed, unauthorized in Wave V1 |

## 2. Backfilled Top-3 decision record (Correction 1)

The strategy mission (`PRE_HUMAN_VISUAL_COMPLETION_STRATEGY_v1.md`) did not
execute its required Top-3 comparison because Track A had already been
owner-selected before that checkpoint began. This record backfills that
comparison without reopening the strategy's terminal disposition.

**Candidates:**

- **A. Wave V1 evidence completion and visual route truth** — recapture the
  full canonical screen/state matrix, classify and close S3, add
  reduced-motion capture, publish a symptom ledger, select the Wave V2 model.
- **B. Sharky state-art production** — produce the six
  `EXTERNAL_ASSET_INPUT_REQUIRED` state-art rows.
- **C. PHP-9 / Human Novice admission** — advance toward
  `PRE_HUMAN_READY` / Human Novice Proof.

**Evaluation:**

| Criterion | A — Wave V1 | B — Sharky art | C — PHP-9 / Human |
| --- | --- | --- | --- |
| Learner-visible EV | Medium-high — closes a directive (`VISUAL_SYSTEM_COMPLETENESS`) that gates premium/human perception of the whole product | Medium — six mood rows on Act0 shell surfaces, real but narrow | High in theory, but gated on unproven visual state |
| Dependency leverage | High — strategy §10 explicitly orders Sharky art *after* Wave V2's layout grammar lands, so B depends on A/Wave V2 | Low — nothing else depends on B landing first | Medium — Human Proof quality depends on a stable, evidenced visual surface |
| Evidence readiness | High — lane #1 tooling exists, strategy already scoped exact groups/devices | Low — blocked on SHK-CREST-01 for crest rows, needs Claude Design production cycle | Low — strategy §11 explicitly found no case for Human observation ahead of Wave V1 |
| Risk of rework | Low if done now; **high if skipped** — placing Sharky assets (B) against layouts that Wave V2 may still change would mean re-placing them | High if B runs before Wave V2 — assets placed against moving containers | High — Human Proof run against S1/S3-open surfaces would be noisy/non-attributable per strategy §11 |
| Defect breadth | Directly answers Correction 2 — the only path that can distinguish "bounded symptom" from "systemic" | N/A — doesn't touch composition | N/A — doesn't touch composition |
| Token/runtime cost | Bounded, 40k–100k, docs+evidence+one diagnostic fix only | External design-tool time, not token-metered the same way, but requires SHK-CREST-01 resolution first (owner-blocked) | Unbounded/unscoped until PHP-9 pre-authorized (it isn't) |
| Opportunity cost of deferring | Low — nothing downstream is blocked by running A first | High — B run first risks wasted production against unstable layout | Low — strategy already recommends "strategy-before-Human" |

**Decision: Wave V1 (A) is Top-1.** Confirmed, not merely expected:

- It is the only candidate that determines whether Model 3 (narrow grammar
  fix) or Model 1 (shared shell) is the correct Wave V2 path — both B and C
  are blocked or degraded without that answer.
- It prevents premature Sharky placement (B) against layouts Wave V2 may
  still move, per strategy §10's explicit ordering.
- Human Proof (C) against incompletely evidenced visual states (S1 open, S3
  unclassified) would produce noisy, non-attributable results per strategy
  §11 — running C before A would waste the one Human session this campaign
  treats as scarce.

B is Top-2 (real work, but correctly sequenced after Wave V2, and
independently gated on SHK-CREST-01). C is Top-3 (highest nominal EV but
lowest current readiness and explicitly not recommended yet by the strategy
itself).

This confirms, rather than reopens, the strategy's Track A selection.

## 3. Scope authorized by this dispatch

Exactly Wave V1 per `PRE_HUMAN_VISUAL_COMPLETION_STRATEGY_v1.md` §13 and its
downstream Prompt 1: acquisition matrix inventory, full deterministic capture
matrix (compact/tall/large), reduced-motion capture tooling addition, S3
classification and closure (fixture, capture-state, or one bounded real
product fix inside the presentation-closure owner cone only), a bounded
real-app simulator parity check, a full symptom/root-cause ledger, and
selection of the Wave V2 model. No Wave V2 or Wave V3 execution.

## 4. Continuation

This dispatch is self-contained authority for Wave V1's remaining packets
(V1-A through V1-F) and Publication B. No further docs-only dispatch is
required mid-wave; the terminal report (Publication B) reconciles this
dispatch's merge SHA alongside Wave V1's own findings.
