name: file_picker_linux
description: Stubbed Linux implementation for tests.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  file_picker: ^6.2.1
  flutter:
    sdk: flutter

flutter:
  plugin:
    implements: file_picker
    platforms:
      linux:
        dartPluginClass: FilePickerLinuxStub
