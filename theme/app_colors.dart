import 'package:flutter/material.dart';

/// Minimal color tokens used by v2 UI components.
/// Extend carefully to avoid breaking existing references.
abstract class AppColors {
  static const Color background = Color(0xFF0F1115);
  static const Color cardBackground = Color(0xFF191D24);
}
