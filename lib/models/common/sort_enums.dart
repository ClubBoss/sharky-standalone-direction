/// Shared sorting enums reused across legacy tooling and modern widgets.
enum ListSortOption { dateNew, dateOld, rating, difficulty, comment }

/// Sorting options for quick preset actions in training spot lists.
enum QuickSortOption { id, difficulty, rating }

/// Determines the ordering for rating-based filters.
enum RatingSortOrder { highFirst, lowFirst }
