import 'player_profile_explanation_layer.dart';

Future<void> showExplanationLayer(
  dynamic context,
  PlayerProfileExplanationData data,
) async {
  throw UnsupportedError(
    'PlayerProfileExplanationLayer requires a Flutter runtime.',
  );
}

Future<void> showTutorialOverlay(dynamic context) async {
  throw UnsupportedError(
    'PlayerProfileExplanationLayer tutorial requires Flutter runtime.',
  );
}
