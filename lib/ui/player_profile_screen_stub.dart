import 'player_profile_models.dart';

dynamic buildPlayerProfileScreen(PlayerProfileData data) {
  throw UnsupportedError('PlayerProfileScreen requires Flutter runtime.');
}

Future<void> showPlayerProfileTutorial(
  dynamic context,
  PlayerProfileData data, {
  Object? tickerProvider,
}) async {
  throw UnsupportedError(
    'PlayerProfileScreen tutorial requires Flutter runtime.',
  );
}
