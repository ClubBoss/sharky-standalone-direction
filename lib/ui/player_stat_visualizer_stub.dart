import 'player_stat_visualizer_models.dart';

Future<void> showStatGain(dynamic context, StatGainEvent event) async {
  throw UnsupportedError(
    'PlayerStatVisualizer.showStatGain requires Flutter runtime.',
  );
}
