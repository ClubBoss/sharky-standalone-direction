import 'player_trait_visualizer_models.dart';

Future<void> showTraitGain(dynamic context, TraitGainEvent event) async {
  throw UnsupportedError(
    'PlayerTraitVisualizer.showTraitGain requires Flutter runtime.',
  );
}
