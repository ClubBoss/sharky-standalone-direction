import 'package:flutter/material.dart';

/// Stub file - not currently used in the application.
/// This file references private members from PokerAnalyzerScreenState
/// that don't exist in the current implementation.
/// TODO: Remove or implement when poker analyzer refactoring is complete.

class ActionEditor extends StatelessWidget {
  ActionEditor({super.key});

  @override
  Widget build(BuildContext context) => const Center(
    child: Text(
      'ActionEditor widget is not yet implemented',
      style: TextStyle(color: Colors.white70),
    ),
  );
}
