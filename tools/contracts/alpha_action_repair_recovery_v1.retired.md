# Alpha Action Journey Contract v1 — retired

`alpha_action_repair_recovery_v1` was retired after real Simulator Computer Use
proved that a fresh learner reaches Learn with `First Table Guide` current and
`Fold, check, call, raise` locked. It must not be restored or cited as a
fresh-install direct route.

Replacement contracts:

- `alpha_fresh_install_first_lesson_v1`
- `alpha_action_repair_recovery_unlocked_v2`

The Action capability remains valid after its explicit, production-equivalent
prerequisite progression state is satisfied.
